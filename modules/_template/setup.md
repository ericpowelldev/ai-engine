# <Module> Setup

<!-- OPTIONAL. Module-specific setup steps, run by /setup when this module is in scope.
     Org-specific environment prep lives here: credentials pointers, local services,
     extra tooling. Delete this file if the module needs no setup beyond wiring. -->

Example steps (replace with your own):

1. Verify <the org's CLI / local stack> is installed; if not, point the user at <install doc>.
2. Confirm the environment file at `<path>` exists with the keys `<A>`, `<B>` (never read or print their values).
