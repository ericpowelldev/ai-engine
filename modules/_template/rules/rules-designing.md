# Designing Rules

<!-- Rules the agent loads before UI, UX, mockup, or visual design work. -->

Example entries (replace with your own):

- **designing-tokens-only** — Colors, spacing, and type come from the design system's tokens; hardcoded values are reserved for one-off emphasis and carry a comment saying why.
- **designing-states-before-polish** — Every component ships empty, loading, and error states before any visual refinement; a beautiful component that renders nothing on an empty list is unfinished.
